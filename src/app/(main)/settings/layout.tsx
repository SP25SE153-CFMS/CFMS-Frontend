import { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Cài đặt trang trại',
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return children;
}
